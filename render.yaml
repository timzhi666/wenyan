services:
  - type: web
    name: wenyan-test
    runtime: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && python app.py"
    envVars:
      - key: PORT
        value: 10000
        sync: false
